# cdmaestas.github.io

Source for my GitHub Pages site at https://cdmaestas.github.io — a landing page describing the HPC and IBM Storage Scale projects I work on.

Plain static HTML, no build step. Edit `index.html` and push to `main`; GitHub Pages redeploys automatically. The `.nojekyll` file disables Jekyll processing.
